const API_CONFIG = {
    BASE_URL: "https://devflorian.cornillet.com",
    ENDPOINTS: {
        REGISTER: '/register',
        LOGIN: '/login',
        LOGOUT: '/logout',
    }
};

export default API_CONFIG;